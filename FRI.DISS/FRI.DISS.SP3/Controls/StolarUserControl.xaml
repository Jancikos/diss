<UserControl x:Class="FRI.DISS.SP3.Controls.StolarUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FRI.DISS.SP3.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="45" d:DesignWidth="120">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="_txt_Id" Text="#1" />
        <TextBlock Grid.Column="1" x:Name="_txt_Workplace" Text="WP0" Margin="10 0 0 0" TextAlignment="Right" />

        <Ellipse Grid.Column="2" x:Name="_elp_IsWorking" Width="8" Height="8" Fill="Red" HorizontalAlignment="Left" Margin="5,0,10,0" />

        <TextBlock Grid.Column="3" x:Name="_txt_WorkRatio" Text="56,25%" Margin="5 0 0 0" FontSize="10"  TextAlignment="Right" Visibility="Collapsed"/>
    </Grid>
</UserControl>

